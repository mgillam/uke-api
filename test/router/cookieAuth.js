const router = require('../../src/router/cookieAuth.router');
const { spy } = require('sinon');

describe('#cookieAuthRouter', function() {
  it.skip('should return a set-cookie header with a jwt on auth', function() {});
  it.skip('should show status when token is valid');
  it.skip('should return an error when token is invalid');
});
