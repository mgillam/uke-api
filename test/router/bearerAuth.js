const router = require('../../src/router/bearerAuth.router');
const { spy } = require('sinon');

describe('#bearerAuthRouter', function() {
  it.skip('should return a jwt on auth', function() {});
  it.skip('should show status when token is valid');
  it.skip('should return an error when token is invalid');
});
